---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: calculator-app
    k8s-app: calculator-appp
  name: calculator-app
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  type: LoadBalancer # ClusterIP # LoadBalancer # NodePort
  selector:
    app: calculator-app
